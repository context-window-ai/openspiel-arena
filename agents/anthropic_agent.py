"""
Anthropic LLM agent — prompts a Claude model to choose a move.

Reads ANTHROPIC_API_KEY and ANTHROPIC_MODEL from the environment (via .env).
"""

# TODO: implement AnthropicAgent(BaseAgent) using the anthropic Python SDK
